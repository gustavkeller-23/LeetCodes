#include <stdio.h>
#include <stdlib.h>

#define TAM_ARRAY 6

char *longestSubstring(char array[]){
    char *newArray = (char *) malloc (TAM_ARRAY * sizeof(char));
    
    return newArray;
}

int main(){
    

    return 0;
}